{
  "id": "maic",
  "name": "MAIC Provider",
  "description": "Minimal MAIC provider plugin: injects tool_execution='client' into outbound OpenAI-compatible chat completion requests so MAIC returns tool_calls for client-side execution. Plus per-model extraParams passthrough from openclaw.json.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {}
  }
}
