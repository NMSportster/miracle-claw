{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Miracle Claw main window — spawn the launcher sidecar to boot the bundled chat gateway, then host the chat UI at localhost:28789/. The launcher sidecar is the only executable the webview is permitted to invoke; the bundled Node runtime is kept off the permission boundary by being exec'd FROM the launcher rather than directly.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "miracle-claw-launcher",
          "sidecar": true,
          "args": [
            "--gateway-port",
            {
              "validator": "^[0-9]{4,5}$"
            }
          ]
        }
      ]
    }
  ]
}
